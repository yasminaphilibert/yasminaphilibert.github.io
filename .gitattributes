# Videos are deliberately NOT in Git LFS. GitHub Pages serves whatever the
# build produced, and an LFS-backed checkout without `lfs: true` yields 130-byte
# pointer files that get published as the videos — which broke every video on
# the site. Plain git objects also keep the free LFS bandwidth quota out of the
# deploy path. Keep each file under GitHub's 100 MB limit.
